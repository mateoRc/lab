services:
  vault:
    build:
      context: ${VAULT_BUILD_CONTEXT:-../vaultsh}
    ports:
      - "${VAULT_PORT:-8080}:8080"
    environment:
      CONTENT_PATH: /app/content
      ATLAS_URL: http://atlas:8080
      FORGE_URL: http://forge:8080
      TELEMETRY_QUEUE_SIZE: ${TELEMETRY_QUEUE_SIZE:-1000}
    volumes:
      - ./content:/app/content:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  atlas:
    build:
      context: ${ATLAS_BUILD_CONTEXT:-../atlas}
    ports:
      - "${ATLAS_PORT:-8081}:8080"
    environment:
      ATLAS_CONTENT_ROOT: /app/content
      FORGE_URL: http://forge:8080
      TELEMETRY_QUEUE_SIZE: ${TELEMETRY_QUEUE_SIZE:-1000}
    volumes:
      - ./content:/app/content:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  forge:
    build:
      context: ${FORGE_BUILD_CONTEXT:-../forge}
    ports:
      - "${FORGE_PORT:-8082}:8080"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/healthz')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
