services:
  vault:
    image: ${VAULT_IMAGE:-vaultsh:local}
    build:
      context: ${VAULT_BUILD_CONTEXT:-../vaultsh}
    ports:
      - "${VAULT_PORT:-8080}:8080"
    environment:
      CONTENT_PATH: /app/content
      ATLAS_URL: http://atlas:8080
      FORGE_URL: http://forge:8080
      ATLAS_AUTH_TOKEN: ${ATLAS_AUTH_TOKEN:?set ATLAS_AUTH_TOKEN in .env}
      FORGE_AUTH_TOKEN: ${FORGE_AUTH_TOKEN:?set FORGE_AUTH_TOKEN in .env}
      TELEMETRY_QUEUE_SIZE: ${TELEMETRY_QUEUE_SIZE:-1000}
      DEPLOYMENT_METADATA_PATH: /app/runtime/deployment.json
      SENTINEL_METADATA_PATH: /app/runtime/sentinel.json
    volumes:
      - ./content:/app/content:ro
      - ./runtime:/app/runtime:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  atlas:
    image: ${ATLAS_IMAGE:-atlas:local}
    build:
      context: ${ATLAS_BUILD_CONTEXT:-../atlas}
    environment:
      ATLAS_CONTENT_ROOT: /app/content
      ATLAS_AUTH_TOKEN: ${ATLAS_AUTH_TOKEN:?set ATLAS_AUTH_TOKEN in .env}
      FORGE_URL: http://forge:8080
      FORGE_AUTH_TOKEN: ${FORGE_AUTH_TOKEN:?set FORGE_AUTH_TOKEN in .env}
      TELEMETRY_QUEUE_SIZE: ${TELEMETRY_QUEUE_SIZE:-1000}
      DEPLOYMENT_METADATA_PATH: /app/runtime/deployment.json
    volumes:
      - ./content:/app/content:ro
      - ./runtime:/app/runtime:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  forge:
    image: ${FORGE_IMAGE:-forge:local}
    build:
      context: ${FORGE_BUILD_CONTEXT:-../forge}
    environment:
      FORGE_AUTH_TOKEN: ${FORGE_AUTH_TOKEN:?set FORGE_AUTH_TOKEN in .env}
      FORGE_DATABASE_PATH: /app/data/forge.db
      FORGE_RETENTION_DAYS: ${FORGE_RETENTION_DAYS:-30}
      FORGE_MAX_DATABASE_BYTES: ${FORGE_MAX_DATABASE_BYTES:-134217728}
    volumes:
      - forge_data:/app/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/healthz')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  forge_data:
